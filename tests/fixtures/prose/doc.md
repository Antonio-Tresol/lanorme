# Notes

We optimize the color of the widget — it looks great ✅

Use `color = "red"` inline; the fenced block below must be ignored:

```python
color = "blue"
optimize()
```

Plain prose: the behavior is analyzed carefully.
